
import { Wrench, Briefcase, CloudCog, ShieldCheck, type LucideIcon } from 'lucide-react';

export const iconMap: { [key: string]: LucideIcon } = {
  Wrench,
  Briefcase,
  CloudCog,
  ShieldCheck,
  // Add other icons used by services here if needed
};
